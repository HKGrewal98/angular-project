export interface ContactDetails {
    address: string;
    phone: string;
    email: string;
  }
  
export interface FormData {
    name: string;
    email: string;
    subject: string;
    message: string;
}